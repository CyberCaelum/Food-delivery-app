<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "com.sky.mapper.DishMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description,status) value
        (#{name},#{categoryId},#{price},#{image},#{description},#{status})
    </insert>

    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select dish.*,category.name as category_name from dish left join category on dish.category_id = category.id
        <where>
            <if test="name != null and name != ''">
                dish.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and dish.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and dish.status = #{status}
            </if>
        </where>
    </select>

    <update id="update">
        update dish 
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
                description = #{description},
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="price != null">
                price  = #{price},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>
</mapper>